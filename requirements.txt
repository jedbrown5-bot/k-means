# Pinned to the tested version so the deployed app behaves exactly as tested.
streamlit==1.60.0
numpy>=1.24
matplotlib>=3.7
pandas>=1.5
# Optional: enables the "Click to place" centre mode. Without it, "Type coordinates" still works.
streamlit-image-coordinates>=0.1.9
